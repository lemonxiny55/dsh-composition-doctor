# Release evidence: dsh-composition-doctor 0.1.3

Recorded 2026-09-17 before any tag or npm publish. This evidence describes the local packed artifact; it is not post-publish evidence.

## Package contract

`package.json` and the packed `package/package.json` matched for:

- version: `0.1.3`
- files: `dist`, `cordis.patch.yml`, `README.md`, `README.zh.md`, `CHANGELOG.md`, `LICENSE`
- bin: `dsh-doctor -> ./dist/cli/main.js`
- engines: `node >=20`
- repository: `https://github.com/lemonxiny55/dsh-composition-doctor.git`
- license: `MIT`
- exports: `.`, `./client`, `./cli`, and `./package.json`

Packed artifact:

- path: `.tmp/release-pass-pack/dsh-composition-doctor-0.1.3.tgz`
- contents: 34 release files; no test directory, `.tmp`, `node_modules`, or lockfile
- SHA-512: `f455ebe5fb1fc2a98efe69e4aacb5b617a6d17933c1273dc24be8fadb74a0a668d0ab4404f1cbd6e3380d5bda25d27ed78aa72a6365b5ede33434e7311c4eabc`
- integrity: `sha512-9FXr5fsfwqmO/mnkqstbYXptF5M8EnPcJL6PrbdKCmaNCrRATxy9bjOA1b2iXSfteKpypjZbXt4zQ05zEcTqvA==`

## Fresh user-view smoke

In a new `.tmp/release-pass-install` directory, with lifecycle scripts disabled:

```text
pnpm add file:dsh-composition-doctor-0.1.3.tgz --ignore-scripts --no-lockfile
dsh-doctor --version                    -> 0.1.3, exit 0
dsh-doctor scan --profile test/fixtures/preflight-real-profile --format both --output user-report --fail-on never
                                           -> exit 0
                                           -> report.json and report.md written
                                           -> stderr empty
                                           -> evidenceMode=static
```

## Test gate

The final command used the locally obtained official rc.1/rc.2 bins and explicitly enabled the online resolver regression:

```text
pnpm test       -> 22 test files passed; 89 passed, 2 skipped, 0 failed
pnpm typecheck  -> passed
pnpm build      -> passed
```

The two skips are Windows file-symlink tests unavailable to the current test process. They are not compatibility passes.

The real DSH harness passed 5/5 for the public CLI behavior of `@deepseek-ai/dsh@0.1.5-rc.1` and `@deepseek-ai/dsh@0.1.5-rc.2`. The online resolver test passed with exact version, manifest, integrity/hash, cache cleanup, and no lifecycle execution.

## Evidence boundaries

- `static` remains metadata/patch evidence only.
- `composed` comes from public `dsh --dump-config`; it is not runtime verification.
- `runtime-observed` was not produced; runtime smoke remains `not-run` because no trusted OS-level isolation backend is available.
- `0.1.6-alpha.1` is expected-compatible/experimental only, not verified.
- A temporary directory is not a security sandbox.
- No npm publish or tag operation was performed.

Post-publish smoke is intentionally pending until an actual npm-published tarball exists.
